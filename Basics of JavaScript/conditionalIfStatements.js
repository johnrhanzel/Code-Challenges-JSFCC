//use conditinal logic with if statements

function trueOrFalse(wasThatTrue) {
    // Only change code below this line
    if(wasThatTrue){ //if wasThatTrue it will print
      return "Yes, that was true";
    }
    return "No, that was false"; //otherwise it will print this statement
  
    // Only change code above this line
  
  }
  
  trueOrFalse(true);
  trueOrFalse(false);