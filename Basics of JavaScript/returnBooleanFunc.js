//returning boolean values from functions

function isLess(a, b) {
    // Only change code below this line
    return a < b; //remove if else and change to return a < b
    // Only change code above this line
  }
  
  isLess(10, 15);